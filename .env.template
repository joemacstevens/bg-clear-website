# BG Clear (Phase 1)
# Use 1Password CLI to inject secrets at runtime:
#   op run --env-file .env.template -- npm run dev

# Vercel personal access token (store in 1Password)
VERCEL_TOKEN={{ op://Dev/Vercel Token (Clawdbot)/token }}

# Formspree endpoint (not secret, but stored in 1Password for consistency)
FORMSPREE_ENDPOINT={{ op://Dev/Formspree BG Clear/endpoint }}
